use crate::graph::GraphWrap;

pub fn to_cgraph(
	_graph: &GraphWrap,
	_node_sort: &str,
	_refine_order: &str,
	_use_color: bool,
	_use_labels: bool,
	_debug: bool,
) -> String {
	"scott-rs-placeholder".to_string()
}
